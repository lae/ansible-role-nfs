---
nfs_exports:
  - path: /var/log
    exports:
      - "*(ro)"
  - path: /home
    exports:
      - "{{ inventory_hostname | replace('server', 'client') }}(rw)"
      - "127.1.1.1(ro)" # doesn't exist, but used for testing multiple exports
  - path: /backups
    exports:
      - "{{ inventory_hostname | replace('server', 'client') }}(rw,no_root_squash)"
