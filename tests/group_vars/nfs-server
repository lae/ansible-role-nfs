---
nfs_exports:
  - path: /var/log
    exports:
      - "*(ro)"
  - path: /home
    exports:
      - "{{ inventory_hostname | replace('server', 'client') }}(rw)"
      - "deafbeef.local(ro)" # I have no idea, just random test data? lol
  - path: /backups
    exports:
      - "{{ inventory_hostname | replace('server', 'client') }}(rw,no_root_squash)"
